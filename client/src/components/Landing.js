import React from "react";

const Landing = () =>
  <div style={{ textAlign: "center" }}>
    <h1>Emaily</h1>
    Collect feedback from your users
  </div>;

export default Landing;

// remember, with only one expression in the function, there's no
// need for curly braces and return statement.
